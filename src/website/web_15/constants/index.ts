import { DimensionProps, RightTopProps } from "../types";

export const DEFAULT_DIM: DimensionProps = {
  width: 0,
  height: 0,
};

export const GAP = 10;

export const DEFAULT_RIGHT_TOP: RightTopProps = {
  right: 0,
  top: 0,
};
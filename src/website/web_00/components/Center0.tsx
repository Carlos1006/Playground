import { FC } from "react";
import globalCss from "../styles/main.module.scss";

const Center0: FC = () => {
  return <div data-slot="c0000-00" className={globalCss.slot} />;
};

export default Center0;

import { FC } from "react";
import globalCss from "../styles/main.module.scss";

const Center1: FC = () => {
  return <div data-slot="c1000-00" className={globalCss.slot} />;
};

export default Center1;
